<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:title="@string/add_new_customer"
        android:id="@+id/add_customer_menu"
        android:icon="@drawable/add_person"
        app:iconTint="@color/icon_default"
        app:showAsAction="always"
        />
    <item android:title="@string/add_new_tool"
        android:id="@+id/add_tool_menu"/>

    <item android:title="Reset DB"
        android:id="@+id/menu_reset"/>


</menu>